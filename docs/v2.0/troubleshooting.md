# Troubleshooting (v2.0 Legacy)

See [v2.1 troubleshooting](../v2.1/troubleshooting.md) for the current guide.

## v2.0 notes

- `Wrap="True"` was a boolean attribute.
- Default orientation was **Horizontal** (v2.1 defaults to vertical column).
- `Justify` used `Start` / `End` instead of `FlexStart` / `FlexEnd`.
